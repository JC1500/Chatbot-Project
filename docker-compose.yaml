version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis_container
    ports:
      - "6379:6379"
    restart: always

  streamlit_app:
    build: .
    container_name: modelchatbot
    ports:
      - "8501:8501"
    depends_on:
      - redis
    restart: always